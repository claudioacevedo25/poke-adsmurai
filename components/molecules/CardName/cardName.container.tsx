import { CardNameComponent } from "./cardName.component"
import { CardNameProps } from "./cardName.model"

export const CardNameContainer = (props: CardNameProps) => (
  <CardNameComponent {...props} />
)
